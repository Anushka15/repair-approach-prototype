select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 101 and s1.university = 'UT' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 102 and s1.university = 'UT' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 103 and s1.university = 'EIND' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 104 and s1.university = 'EIND' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 105 and s1.university = 'AMS' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 101 and s1.university = 'EIND' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 103 and s1.university = 'AMS' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 106 and s1.university = 'UT' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 102 and s1.university = 'AMS' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SID  = 104 and s1.university = 'UT' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'John Bell' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Claire Stevens' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Pet Norton' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'George Bailey' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Leah Murphy' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'David Wong' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Maria Rossi' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Rahul Mehta' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Aisha Khan' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.SName = 'Sofia Janssen' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.University  = 'UT' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.University  = 'AMS' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID, s1.SName ,s1.University ,s1.Address ,s1.EmailId ,s1.PhoneNum ,s1.Postcode, s1.age  FROM prototype_fp.students s1  WHERE s1.University  = 'EIND' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.University,s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.SID,s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.Address,s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.EmailId,s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.PhoneNum,s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

SELECT DISTINCT s1.Postcode,s1.SName  FROM prototype_fp.students s1 where NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode)));

select s1.SID,s1.SName from prototype_fp.students s1 where s1.University  = 'UT' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sid,s1.sname;

select s1.SID,s1.SName from prototype_fp.students s1 where s1.University  = 'AMS' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sid,s1.sname;

select s1.SID,s1.SName from prototype_fp.students s1 where s1.University  = 'EIND' and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sid,s1.sname;

select s1.SName,s1.University from prototype_fp.students s1 where s1.SID = 101 and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sname, s1.university;

select s1.SName,s1.University from prototype_fp.students s1 where s1.SID = 102 and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sname, s1.university;

select s1.SName,s1.University from prototype_fp.students s1 where s1.SID = 103 and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sname, s1.university;

select s1.SName,s1.University from prototype_fp.students s1 where s1.SID = 104 and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sname, s1.university;

select s1.SName,s1.University from prototype_fp.students s1 where s1.SID = 105 and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sname, s1.university;

select s1.SName,s1.University from prototype_fp.students s1 where s1.SID = 106 and NOT EXISTS ( SELECT * FROM prototype_fp.students  s2 WHERE (s1.SID  = s2.SID) and (s1.university = s2.university) AND ((s1.SName  <> s2.SName) or (s1.address  <> s2.address) or (s1.emailid  <> s2.emailid) or (s1.phonenum  <> s2.phonenum) or (s1.postcode  <> s2.postcode))) group by s1.sname, s1.university;
